# kind cluster for Elpio e2e. One control-plane + one worker so node-scaling and
# scheduling behave more like a real cluster.
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: elpio-dev
nodes:
  - role: control-plane
  - role: worker
